# 四川省经济和信息化厅第二次验证记录

## 基本信息

- 来源 ID：`sc-jxt-notices`
- 验证日期：2026-09-16
- 结果：前期访问失败；网络恢复后完成列表和1条详情的轻量复测
- User-Agent：`PolicySourcePilot/0.1 (public-source-validation)`
- 并发数：1
- 是否访问登录系统：否
- 是否绕过访问限制：否

## 第一次尝试

使用 macOS `/usr/bin/curl`（LibreSSL），请求间隔为60秒。

| 时间 | 请求对象 | 结果 |
| --- | --- | --- |
| 13:58:28 | 列表页 | TLS `SSL_ERROR_SYSCALL`，HTTP 000 |
| 13:59:28 | 详情样本1 | TLS `SSL_ERROR_SYSCALL`，HTTP 000 |
| 14:00:29 | 详情样本2 | TLS `SSL_ERROR_SYSCALL`，HTTP 000 |
| 14:01:30 | 详情样本3 | TLS `SSL_ERROR_SYSCALL`，HTTP 000 |

## 第二次尝试

发现用户终端 `(base)` 使用 `/opt/anaconda3/bin/curl`（OpenSSL 3.5.6），因此改用相同客户端重试。

| 时间 | 请求对象 | 结果 |
| --- | --- | --- |
| 14:02:06 | 列表页 | TLS unexpected EOF，HTTP 000 |
| 14:03:07 | 详情样本1 | DNS 解析超时，HTTP 000 |

由于错误从 TLS 握手失败变为 DNS 解析超时，且没有收到任何 HTTP 响应，停止本轮剩余请求。

## 网络恢复后的轻量复测

| 时间 | 请求对象 | 结果 |
| --- | --- | --- |
| 14:51:18 | 列表页 | HTTP 200，34566字节，无重定向 |
| 14:52:12 | 详情样本1 | HTTP 200，31088字节，无重定向 |

根据本轮“简单验证即可”的范围，详情样本2和3没有再次请求。

## 环境证据

- shell 中未设置 `HTTP_PROXY`、`HTTPS_PROXY` 或 `ALL_PROXY` 环境变量。
- macOS 系统 HTTP、HTTPS 和 SOCKS 代理均已启用。
- 系统代理地址为 `127.0.0.1:7897`。
- 本轮失败可能与代理或当时网络状态有关，但现有证据不能单独确定根因。

## 轻量对比结果

- 列表新增：0
- 列表重复且未变化：10
- 列表字段更新：0
- 当前样本缺失：0
- 详情样本1：解析结果、原始HTML和SHA-256均与第一次相同
- 详情样本2和3：本轮未复测
- 观察到的失效：0
- 解析失败：0
- 前期访问失败：共6次，其中列表2次、详情4次；网络失败没有被判定为内容失效

## 本地证据

- 失败请求记录和成功响应均保存在 `local-artifacts/sc-jxt-notices/`，不提交 Git。
- 成功列表快照：`list-run-02.html`
- 成功详情快照：`detail-01-run-02.html`

## 当前结论

第二次运行前期受网络或代理异常影响，恢复后完成了列表和1条详情的轻量复测。列表前10条及详情样本1均未变化，说明现有去重和内容哈希判断在本次复测样本上符合预期；该结论不覆盖未复测的另外2条详情。
